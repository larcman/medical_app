<!DOCTYPE html>
<html>
  <head>
  	<meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>MedicalApp</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  	<% if current_user %>
  	  <header class="navbar navbar-fixed-top navbar-inverse">
	    <div class="container">
	      <% if @research %>
	        <p class="navbar-brand"><%= @research.code %></p>	     
	      <% end %>
	      <a class="navbar-brand"> ENALAPRIL </a> <!-- DELETE ME -->
	      
	      <nav>
	        <ul class="nav navbar-nav navbar-right">
	          <li><%= link_to "Inicio", root_path %></li>
	          <li><a>Personas</a></li>
	          
	          <% if @research %>
	            <li><a href="#">Pacientes</a></li>
	          <% else %>
	            <li><a href="#">Investigaciones</a></li>
	            <% if current_user && current_user.isadmin %>
	              <li><%= link_to "Usuarios", users_path, method: "get" %></li>
	            <% end %>
	          <% end %>
	          
	          <% if current_user %>
	            <li class="dropdown">
	              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <%= current_user.firstname %> <span class="caret"></span></a>
	              <ul class="dropdown-menu">
	                <li><a href="#">Ver Perfil</a></li>
	                <li role="separator" class="divider"></li>
	                <li class="dropdown-header">Acciones</li>
	                <li><%= link_to "Salir", logout_path, method: "delete" %></li>
	              </ul>
	            </li>
	          <% else %>
	            <li><%= link_to "Entrar", login_path %> Log In</li>
	          <% end %>
	        </ul>
	      </nav>
	    </div>
	  </header>
  	<% end %>

    <%= yield %>

  </body>
</html>
